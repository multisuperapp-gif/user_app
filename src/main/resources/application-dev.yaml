spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.msa.userapp: DEBUG
    software.amazon.awssdk: WARN
    org.apache.http: WARN
